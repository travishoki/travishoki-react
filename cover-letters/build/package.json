{
  "name": "travis-hoki-cover-letter-build",
  "version": "1.0.0",
  "private": true,
  "description": "Builds the cover letter PDF, and merges it with a resume PDF when an application has no separate cover letter upload.",
  "scripts": {
    "build": "./build.sh"
  },
  "dependencies": {
    "pdf-lib": "^1.17.1"
  }
}
